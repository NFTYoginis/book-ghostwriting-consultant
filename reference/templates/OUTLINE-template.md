# [Book Title] — Outline

Versioned, dated, reasoned. Never overwrite a prior version in place — append the new one above or link to it, and state what changed and why. Shape modeled on the real `OUTLINE.md` (`GabeYoga-HQ/Detox Book/`), which moved v3 → v3.1 → v4 → v5 this way over one production run.

---

## v[N] — [date]

**Reason for this version:** [what changed and why — e.g., "operator directive: split the densest chapter" or "initial pass from extraction, nothing drafted yet"]

### Structure

```
PART [N] — [part title]
1. [Chapter title] — status: [drafted | ready | needs-material]
   → feeds: [material index section / story bank entries]
2. ...
```

### De-dup ledger

State every supersession explicitly — what moved, what got absorbed into what, what must never be re-drafted because it already lives elsewhere. (Real example: "Ch5's full Mirror section → trimmed to a pointer to Ch7 · do not re-draft science material into Part II.")

### PROTECTED LINES (do not sand down in any revision pass)

```
- "[verbatim author quote]" — source: [transcript ref] — why it matters: [one line]
```

Grows over time as the author flags lines during any stage, not just during outlining.

### Still needed from the author

```
- [specific gap] — blocking [chapter/section] — needs: [what kind of capture would fill it]
```

Never silently emptied. An item leaves this list only when a traceable new capture session fills it.

---

## v[N-1] — (superseded) [date]

[Prior version retained below the current one, same shape, so the outline's own history stays legible — mirrors the real file keeping v3/v4 detail below the authoritative v5 section.]
